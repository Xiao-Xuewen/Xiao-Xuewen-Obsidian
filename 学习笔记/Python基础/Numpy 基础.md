# 一、创建数组
1. 将一个列表变成一个**一维数组**：`a = np.array([10,20,30])`
2. **二维数组**：将一维列表中的元素也替换成列表，也就是列表，列表：
```
img = np.array([[10,20,30,40],

                [20,30,40,50],

                [30,40,50,60]])
```
3. **三维数组**：二维数组中的每一个列表中的单独元素再替换成列表：
```
c = np.array([[[1,2,3],[2,3,4],[3,4,1],[5,6,9]],

             [[112,342,6],[213,24,124],[12,21,12],[123,431,241]]])
2*4*3
c1 = np.array([[[1,2,3],[2,3,4]],

              [[3,4,1],[5,6,9]],

              [[112,342,6],[213,24,124]],

              [[12,21,12],[123,431,241]]])
4*2*3
```
4. 快捷创造数组：
```
a = np.zeros((3,4,5)) 0
b = np.ones((2,4)) 1
c = np.empty((3,3)) 空
d = np.arange(12) 0-11
```
5. 查看**数组属性**：
```
img.shape 数组形状
img.idim 数组维数
img.size 数组元素总数
img.dtype 数组元素数据类型
```
6. 基本的数组**操作**：
```
a**2 平方
a + - * / b 基本的按位置四则运算
a @ b 矩阵乘法
```
7. 随机数生成器
```
rg = np.random.default_rng(seed)
a = rg.random((shape)) 默认生成0-1的float64数据
常用：
a = np.floor(10*rg.random((3,4))) floor 不大于x的最大整数
a 是[0,10)的随机3行4列二维数组
```
# 二、切片
```
一维数组：a = np.arange(10)
         b = a[:6:] #[start:end:step] 切了0-5号元素
二维数组：b = a[3,4] 第三行第四列元素
		 b = a[:3] 所有行 第一列-第三列
		 b = a[:2,:4] 第一行第二行 第一列-第四列
		 b = a[-1] 最后一行
三维数组与二维数组同理 由,分割维度       
```
# 三、形状操作
1. 平铺 `a_ravel = a.ravel()` C-style 平铺，右边的索引变化快 00 01 02 03 10 11 12 13 列变化4个 行变化1个 二维变成一维
2. 修改形状：
```
a = np.arange(12)
(1) b = a.reshape((3,4)) # 生成view,不修改a
(2) a.resize((3,4)) # 修改a本身 没有返回值
(3) a_T = a.T # 转置
```
3. 堆叠：
```
a = np.floor(10*rg.random(4))
b = np.floor(10*rg.random(4))
c = np.vstack((a,b)) #垂直堆叠
d = np.hstack((a,b)) #水平堆叠
print(c,d)

[[9. 2. 8. 1.]
 [5. 1. 6. 8.]] 
 
[9. 2. 8. 1. 5. 1. 6. 8.]

e = np.column_stack((a,b)) #针对一维数组 把一维数组当作一列来处理 虽然也是“水平”排列                  
print(e)                   #但结果与hstack不同

[[9. 5.]
 [2. 1.]
 [8. 6.]
 [1. 8.]]

f = np.hstack((a[:,newaxis],b[:,newaxis])) # 增加列的维度
# 变成列向量(2维)结果和colnmu_stack相同
# 先把a,b增加一个列向量 然后水平拼接
print(f)

[[9. 5.]
 [2. 1.]
 [8. 6.]
 [1. 8.]]
#newaxis 新的维度
```
4. 切割
```
a = np.floor(10 * rg.random((2,12)))
b = np.hsplit(a,3) # 按水平方向 竖着切
c = np.vsplit(a,2) # 按垂直方向 横着切
# 3 2 是切割成几个数组
```
# 四、View and Copy
1. **对象相同 内存共用**：`b = a` 二者说白了就是一个对象一个内存数据的两个名字
2. **对象不同 内存共用**：`b = a.reshape((3,4))` 共用内存 但是对象不同 也就是View相当于b从另外一个“角度”来看这个共同内存中的数据
3. **对象不同 内存不共用**：`b = a.copy()`，b和a彻底的独立 修改任何一个不影响另一个
4. 当有一份**巨大的数据量**时 比如10000个数据 我们只要前100 我们如果只是切片b = a[:100] 然后删除 a 实际上并没有释放 a 的10000的数据内存 因为b的视图还在指向 我们需要 b = a[:100].copy() 之后 del a 才会释放10000的数据的内存
# 五、广播
1. 广播的基本条件：
```
#广播的两个条件满足一个即可：
1）两个数组的shape右对齐 
2）如果对齐同 其中有1也行

# (4,3) (3,)可以 (4,3)(4,)不行 (15,3,5)(3,5)可以 缺失的维度当作1
# img.shape = (256,256,3) scale.shape = (3,) scale=([1.0,0.8,0.5])
# img*scale = img的第三轴与scale对应元素相乘 第一轴和第二轴不变

```
2. 广播的典型用途：
```
#广播典型的用途 批量运算 shape = (4,2) 和 (2,)计算 左边四个2元素向量和基准2元素向量的差
#直接做差即可 得到shape = (4,2) 每一行元素就是对应的差值 不需要for
```
3.显微成像例子：
```
#以显微成像的例子来说 (100,512,512) 理解为 100张图 每张图的高和宽都是 512*512的
#然后每张图的均值mean计算出来 那就是100个数值 shape = (100,)
#然后直接减法不行 因为最右边100和512对不上 需要增加两个维度shape =(100,1,1)
#使用np.newaxisok或者[:,none,none] 变成(100,1,1) 然后做差
#现在mean是个三维数组 我的理解就是行和列都是 1 然后垂直于屏幕的轴有100
#然后原来的图像行和列是 512*512 垂直于屏幕的轴也是100
#实际的减法就是原图像数组的第n个图像的行和列的每一个点都减去mean数组的对应的第n的图像的mean
#最后的shape 依然是(100,512,512) 此时 这100张图像的每一个图像的每一个像素都减去了对应图像的均值
#得到新的100张512*512的图
```
广播的使用条件不是关键 **关键是知道广播是如何计算的 然后理解 设计广播**

1)减均值 如上

2)减同一张图像(512，512)的[none,:,:]做差即可

3)每一个图像的每一行减去一个对应行数的均值(100,512)-->[:,:,none]-->(100,512,1)

**shape完全相同的 简单 对应的+ - * /**
**shape不同的 沿着相同的位置轴上做运算 不同的轴（变化慢） 这句话有点抽象**

以3)为例 我们要把每个图像的每一行减去对应的均值 实际上是在沿着行做运算 (a11-am1 a12-am1 a13-am1 。。。) 列的序号增大的快 也就是沿着行做运算 行号变化慢 也就是沿着列变化慢
所以我们none的位置是列的位置 而不是[100,1,512]虽然这样也能广播 但这样的广播就不对了
这样做差的话实际上是沿着行运算 把每一列的元素减去对应的每一行的均值
听着有些绕 实际上是因为这是一个正方形例子 刚好这么设计也能计算
如果这是一个h w不相等的 比如(100,500,700)初始图像
取每一个图像的每一行的均值(100,500) 怎么变 一定是[:,:,none]因为如果中间none 广播不了
但根据这种设计的话 是根据广播规则来设计的 不是根据计算原理来设计的
例子2) 同时减去同一张图像 每一张图像的行列都要对应减 然后下一张图片 垂直与屏幕的轴变化慢 所以[none,:,:]


   